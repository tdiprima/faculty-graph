[project]
name = "faculty-graph"
version = "0.1.0"
description = "harvests public faculty publication data"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
review = ["pyyaml>=6.0"]
llm = ["anthropic>=0.30.0"]
all = ["pyyaml>=6.0", "anthropic>=0.30.0"]
